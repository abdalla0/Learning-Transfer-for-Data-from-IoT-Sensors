from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project aims to build machine learning models to classify the failure status of sensors and detect anomalies',
    author='Mustafa Abdallah',
    license='MIT',
)
